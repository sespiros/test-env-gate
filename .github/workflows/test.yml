name: test-environment-gate

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build succeeded"

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: release-publish
    steps:
      - run: echo "Published! This only runs after approval."
